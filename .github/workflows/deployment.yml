name: deployment

on:
  workflow_dispatch:
  create:
    tags:
      - /^\d+\.\d+(\.\d+)?(-\S*)?$/

jobs:
  create-github-release:
    name: create-github-release
    steps:

  build-release:
    name: build-release
    needs: [create-github-release]
    steps:
